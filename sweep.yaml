method: bayes
metric:
  goal: minimize
  name: val_loss
parameters:
  augmentation:
    values:
      - false
      - true
  batch_size:
    value: 64
  epochs:
    value: 100
  f1_scoring:
    distribution: categorical
    values:
      - weighted
      - macro
  learning_rate:
    max: 0.01
    min: 0.001
  model:
    values:
      - efficientnet
  optimizer:
    values:
      - Adam
      - RMS
  sample_size:
    max: 0.5
    min: 0.25
  size:
    value: 224
program: train.py