method: bayes
metric:
  goal: minimize
  name: val_loss
parameters:
  augmentation:
    values:
      - true
  batch_size:
    value: 64
  epochs:
    value: 100
  f1_scoring:
    distribution: categorical
    values:
      - weighted
      - macro
  learning_rate:
    max: 0.0005
    min: 0.000005
  model:
    values:
      - efficientnet
  optimizer:
    values:
      - Adam
      - RMS
  sample_size:
    max: 0.5
    min: 0.25
  size:
    value: 224
program: train.py